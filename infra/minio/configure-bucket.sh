#!/bin/sh
# PayTrace MinIO init placeholder (M0a).
#
# Bucket creation is currently handled inline by the `minio-init` service
# in docker-compose.yml. This script is reserved for future bucket policy,
# lifecycle, or quota setup that should run after the bucket exists.
echo "[M0a] MinIO bucket policy script reserved for future use."
