# Demo 脚本

> 状态：**占位** —— 将在 M3 / M4 填充。

三分钟 Demo 演示流程覆盖：

1. `mixed_failure` 场景：创建 Incident → 异步诊断 → SSE 进度 →
   同时给出 Benefit 摩擦与渠道超时双根因，附 Evidence 卡片与 Tool Trace。
2. `data_gap` 场景：报告返回 NEEDS_DATA 并明确列出缺失字段。
3. `normal` 场景：不出现无证据的高置信根因。
4. Eval Lab：规则基线批量评测、逐场景指标、badcase 列表。
