# PayTrace 产品需求文档（PRD）

> 项目名称：PayTrace｜支付转化漏斗异常归因与诊断 Agent  
> 版本：v1.0  
> 状态：开发前讨论稿  
> 项目性质：个人长期迭代项目，首期目标为可演示、可评测的简历 MVP

## 1. 项目概述

PayTrace 面向电商平台、支付服务商及具备复杂支付链路的中大型互联网商户，诊断从订单确认到最终完成支付过程中发生的订单流失。

系统统一分析支付漏斗、优惠选择行为、支付事件、渠道状态与配置变更，识别技术故障、支付决策摩擦等多种原因，输出带证据的损失拆解、根因候选、缺失数据及处置建议。

首期采用模拟支付数据与故障注入场景开发，不接入真实支付渠道，不处理银行卡号等敏感信息。

## 2. 产品目标

### 2.1 核心目标

当完成支付订单数或支付完成率异常下降时，帮助支付产品与运营人员回答：

1. 订单主要流失在哪个支付环节？
2. 相比正常基线少完成了多少订单？
3. 哪些用户、地区、支付方式、渠道或优惠规则贡献最大？
4. 技术失败与支付决策摩擦分别造成了多少影响？
5. 当前证据支持哪些根因，哪些部分仍无法解释？
6. 下一步应该排查、修复或验证什么？

### 2.2 技术目标

项目重点体现两项 AI 应用工程能力：

- Diagnostic Harness：受控的多步工具调用、上下文构建与证据校验。
- Evaluation & Optimization：基于故障注入 Ground Truth 的评测、回归与优化闭环。

辅助体现后端数据处理、Tool Calling、Context Engineering、Trace、Artifact 和反馈回流能力。

## 3. 目标用户

### 3.1 主要用户

- 电商或互联网平台的支付产品人员。
- 支付运营、商户技术支持人员。
- 支付服务商的商户成功与技术支持团队。

### 3.2 适用企业

- 电商、本地生活、票务、酒店与航旅平台。
- 游戏、内容、SaaS及订阅类产品。
- 同时接入多种支付方式或多条支付渠道的中大型商户。
- 聚合支付、支付网关与跨境支付服务商。

### 3.3 非目标用户

- 仅使用单一收款码的小型店铺或个体商户。
- 缺少支付事件数据、交易量不足以形成稳定基线的商户。

## 4. 核心概念与指标

### 4.1 支付转化漏斗

```text
订单确认
→ 进入收银台
→ 展示支付方式与优惠
→ 选择支付方式
→ 发起支付
→ 授权或认证
→ 支付渠道返回结果
→ 平台确认支付状态
→ 订单支付完成
```

取消和重下单作为支付方式选择后的分支：

```text
用户取消
→ 是否重新下单
→ 是否更换支付方式
→ 是否最终完成支付
```

### 4.2 购买意图

`purchase_intent_id` 表示一次真实购买意图。同一用户在限定时间内对基本相同的商品组合取消并重新下单，可归入同一个购买意图，避免将一次购买行为错误统计为多个独立用户流失。

### 4.3 核心指标

| 指标 | 定义 |
| --- | --- |
| 完成支付订单数 | 最终进入已支付状态的订单数量 |
| 订单支付完成率 | 完成支付订单数 / 确认订单数 |
| 购买意图完成率 | 完成支付的购买意图数 / 有效购买意图数 |
| 支付发起率 | 发起支付订单数 / 到达收银台订单数 |
| 支付受理成功率 | 支付成功次数 / 支付发起次数 |
| 取消后重下单率 | 重新下单数 / 支付阶段取消订单数 |
| 重下单恢复率 | 重下单后完成支付数 / 重下单数 |

### 4.4 优惠差额

```text
Benefit Gap = 实际选择支付方式的应付金额 - 最优可用支付方式的应付金额
```

Benefit Gap 仅作为支付决策摩擦的观察证据，不能单独证明用户因优惠差额取消订单。

## 5. 核心业务场景

某次版本与支付活动上线后，订单确认数基本稳定，但最终完成支付订单数明显下降。系统需要：

1. 定位支付漏斗中的主要流失阶段。
2. 拆解不同原因造成的订单损失。
3. 下钻受影响的支付方式、渠道、地区、用户与优惠规则。
4. 结合配置变更和支付事件形成根因候选。
5. 保留暂无法解释的损失，不强行生成完整结论。

同一次 Incident 可以同时包含：

- 优惠展示或支付方式选择摩擦。
- 3DS、风控或授权认证失败。
- 支付渠道超时或不可用。
- 回调丢失、签名失败或状态乱序。
- 用户余额不足、卡片过期等正常业务失败。
- 数据缺失或统计口径错误。

## 6. 根因分类

| 漏斗阶段 | 根因示例 |
| --- | --- |
| 收银台至支付发起 | 优惠未突出展示、支付方式选错、优惠互斥、金额刷新异常、可用条件不清 |
| 支付发起至授权认证 | 3DS失败、风控误拒、额度不足、卡片过期、用户放弃认证 |
| 支付渠道处理 | 渠道不可用、接口超时、路由错误、币种或地区配置错误 |
| 支付结果至订单完成 | Webhook丢失、回调签名失败、状态未更新、重复回调、事件乱序 |
| 数据与统计 | 事件缺失、埋点异常、统计窗口错误、重复或延迟数据 |

国内与跨境支付使用同一诊断内核，国家、币种、发卡行、卡组织、3DS、微信、支付宝、白条等均作为可选支付维度。

## 7. 产品流程

```text
检测完成支付订单异常
→ 创建 Incident
→ 定位主要流失阶段
→ 量化订单损失
→ 按关键维度下钻
→ 查询行为、支付事件与配置证据
→ 生成多根因诊断报告
→ 进行证据一致性校验
→ 人工确认并记录处理动作
→ 观察指标恢复并回流评测
```

## 8. 核心功能模块

### 8.1 模拟数据与故障注入

- 生成用户、购物车、订单、优惠、支付选项、支付尝试及回调事件。
- 支持单根因、多根因、无异常和数据不足场景。
- Ground Truth 单独保存，Agent运行时不可读取。
- 不生成或存储真实银行卡号、账户凭据和个人敏感信息。

### 8.2 漏斗与异常引擎

- 计算支付漏斗及核心转化指标。
- 根据历史基线发现完成支付订单数与转化率异常。
- 计算不同阶段的观察性订单损失。
- 支持按支付方式、渠道、地区、币种、版本等维度下钻。

异常检测与指标计算由确定性代码完成，LLM不负责计算指标或判断是否触发异常。

### 8.3 Incident 管理

状态流转：

```text
DETECTED
→ INVESTIGATING
→ ACTION_REQUIRED
→ MONITORING
→ RESOLVED
```

记录异常范围、诊断版本、处理动作、人工最终根因及恢复结果。

### 8.4 Diagnostic Harness

- 根据漏斗阶段动态选择分析工具。
- 校验工具参数、查询范围和维度。
- 控制最大调用步数、超时、重试和Token预算。
- 将大结果写入Artifact，仅把摘要和引用放入上下文。
- 为工具结果生成唯一Evidence ID。
- 校验每个根因是否绑定有效证据。
- 证据不足时输出缺失数据或未解释损失。
- 保存完整Tool Trace、模型版本、Prompt版本、延迟和成本。

### 8.5 诊断报告

报告包含：

- Incident摘要。
- 支付漏斗损失拆解。
- 受影响范围与主要贡献维度。
- 一个或多个根因候选。
- 每个根因对应的Evidence ID与置信度。
- 替代解释、缺失数据和暂无法解释的损失。
- 技术排查、产品优化或A/B实验建议。

### 8.6 人工复核与回流

用户可以接受、拒绝或修改根因，补充证据，记录处理动作与恢复结果。

反馈优先用于：

- 补充困难样本和回归集。
- 分析Agent失败类型。
- 优化Prompt、工具描述和上下文结构。
- 后续验证Case Memory的实际增益。

首期不宣称系统会根据反馈自动训练或自动改进模型。

### 8.7 Eval Lab

支持对比：

- B0：规则与固定模板基线。
- B1：一次性向LLM提供完整上下文。
- B2：Tool Calling Agent。
- B3：加入Harness、证据校验与上下文工程。
- B4：可选Case Memory实验版本。

## 9. Agent 工具

首期工具控制在七个：

| 工具 | 职责 |
| --- | --- |
| `get_payment_funnel` | 查询各阶段转化率与订单数 |
| `breakdown_conversion_loss` | 计算相对基线的阶段损失 |
| `compare_dimensions` | 按支付方式、渠道、地区等维度下钻 |
| `analyze_benefit_gap` | 分析支付方式优惠与最终应付金额差异 |
| `trace_cancel_and_reorder` | 追踪取消、重下单、方式切换和最终恢复 |
| `inspect_payment_events` | 检查认证、渠道、回调和状态事件 |
| `get_config_changes` | 查询活动、风控、路由、接口及版本变更 |

首期工具全部只读，不提供自动修改路由、配置、优惠或风控策略的能力。

## 10. 核心数据模型

### 10.1 purchase_intent

```text
purchase_intent_id, user_id, cart_fingerprint,
first_order_id, started_at, completed_at, final_status
```

### 10.2 order

```text
order_id, purchase_intent_id, merchant_id, amount,
country, currency, status, created_at, paid_at
```

### 10.3 checkout_session

```text
session_id, order_id, entered_at, exited_at, exit_type
```

### 10.4 payment_option_exposure

```text
session_id, payment_method, channel, display_position,
base_amount, coupon_discount, platform_discount,
method_discount, final_payable, eligible, ineligible_reason
```

### 10.5 payment_attempt

```text
attempt_id, order_id, payment_method, channel,
issuer, amount, status, error_code, latency_ms,
config_version, created_at
```

### 10.6 payment_event

```text
event_id, attempt_id, event_type, event_status,
error_code, occurred_at
```

### 10.7 config_change

```text
change_id, change_type, target, old_value,
new_value, version, changed_at
```

### 10.8 incident/evidence/report

分别保存异常范围与状态、工具证据与Artifact引用、诊断根因及建议。

### 10.9 review_feedback/evaluation_run

分别保存人工最终判断与处置结果、评测版本和指标结果。

## 11. 页面范围

### 11.1 Incident列表

展示异常时间、核心指标、完成支付订单损失、严重程度、状态和当前根因。

### 11.2 诊断工作台

展示支付漏斗、损失拆解、趋势与维度分析、优惠差额、取消重下单路径、支付事件、配置变更、工具Trace、Evidence和诊断报告。

### 11.3 Incident复盘

记录AI诊断、人工最终根因、处理动作、处理前后指标及是否恢复。

### 11.4 Eval Lab

展示各版本评测结果、失败类型、成本、延迟及单场景Trace对比。

首期不开发大型经营驾驶舱和复杂工单中心。

## 12. 评测设计

### 12.1 数据集

- Development Set：开发调试。
- Regression Set：持续回归。
- Holdout Set：不同模板和维度组合。
- Adversarial Set：无关变更、冲突证据、缺失数据和误导线索。

### 12.2 指标

| 指标 | 说明 |
| --- | --- |
| Stage Localization Accuracy | 是否定位正确流失阶段 |
| Root Cause Multi-label F1 | 多根因识别效果 |
| Loss Attribution MAE | 各原因损失订单估算误差 |
| Evidence Precision/Recall | 证据正确性与覆盖率 |
| Unsupported Claim Rate | 无有效证据结论占比 |
| Unknown Calibration | 对未知和数据不足的识别能力 |
| Tool Success Rate | 工具调用成功率 |
| Tool Efficiency | 平均工具调用步数 |
| P95 Latency | 诊断延迟 |
| Token Cost | 单次诊断模型成本 |

## 13. 技术方案

建议技术栈：

- 前端：Next.js、React、TypeScript、Shadcn UI、ECharts。
- 后端：Python、FastAPI、Pydantic、SQLAlchemy。
- 应用数据：PostgreSQL。
- 分析数据：首期可使用PostgreSQL；需要时迁移至Parquet + DuckDB。
- Agent：核心Harness保持框架无关，可采用tRPC-Agent-Python作为运行时。
- 检索：首期使用结构化错误码和规则文档检索，不强制引入复杂向量数据库。
- 工程：pytest、Docker Compose、GitHub Actions。

## 14. 非功能要求

- 诊断工具默认只读，高风险动作必须由人工执行。
- 结构化输出必须通过Schema校验。
- 所有结论可追踪到Evidence与原始Artifact。
- 支持Partial、Failed和数据不足状态，禁止静默失败。
- 记录模型、Prompt、工具和数据版本，保证评测可复现。
- 不使用真实支付凭据、卡号或个人敏感数据。

## 15. 开发阶段

### V0：确定性基线

- 模拟数据与故障注入。
- 支付漏斗、指标及异常检测。
- Incident列表与固定模板诊断。

### V1：Tool Calling Agent

- 七个只读工具。
- Evidence ID、结构化报告和Tool Trace。
- 支持多根因与数据不足输出。

### V2：Harness工程化

- 状态管理、参数校验、预算、超时重试。
- Artifact、上下文裁剪和证据校验。

### V3：Eval与优化闭环

- 多类评测集、批量回归和版本对比。
- 失败分类、Prompt/Tool优化和CI评测。

完成V3后达到简历可展示标准。

### V4：反馈与Case Memory（可选）

- 人工最终根因回流。
- 历史Incident检索。
- Memory开启/关闭A/B评测。
- 只有评测证明有效才进入主链路。

## 16. MVP范围与非目标

### 16.1 MVP必须完成

- 统一支付转化漏斗与购买意图模型。
- 模拟订单、优惠、支付选项、支付事件与配置变更。
- 至少覆盖优惠选择摩擦、认证失败、渠道超时、回调异常和数据不足。
- 支持一次Incident包含多个根因。
- 完成七个分析工具、证据化诊断和人工复核。
- 建立规则、One-shot LLM与Harness Agent的对比评测。

### 16.2 首期不做

- 真实支付渠道接入。
- 支付方式个性化推荐产品。
- 自动分配补贴预算。
- 自动修改支付路由、风控或活动配置。
- 退款、拒付、对账和清结算。
- 完整工单、通知、多租户和复杂权限。
- 多Agent、模型微调和重型实时流处理基础设施。

## 17. 验收标准

MVP至少满足：

1. 可以生成并回放完整订单至支付完成事件。
2. 可以将取消重下单归并至同一购买意图。
3. 可以检测完成支付订单数或支付完成率异常。
4. 可以定位主要流失阶段并展示损失拆解。
5. 可以调用工具分析优惠差额、重下单、支付事件与配置变更。
6. 一次Incident可输出多个根因候选及暂无法解释部分。
7. 每个根因必须引用有效Evidence ID。
8. 数据不足时能够明确输出缺失信息，不强行归因。
9. 用户可以确认或修改最终根因并记录处理结果。
10. Eval Lab可以比较至少三个版本的准确性、可靠性、成本与延迟。

## 18. 项目价值与差异化

PayTrace不将支付异常简单视为技术失败，而是以最终完成支付订单为目标，在同一漏斗中联合分析用户选择、优惠规则、支付技术链路和订单状态一致性。

项目的主要差异化是：

- 通过故障注入构建带Ground Truth的多根因支付Incident，可进行可复现评测。
- 通过证据约束型Diagnostic Harness控制Agent调查过程，允许保留未知与数据不足，降低无依据归因。

